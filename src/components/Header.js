import { View, Text } from "react-native";

export default function Header() {
    return (
        <View className="flex h-[10vh] pt-8 items-center justify-center bg-secondary">
            <Text>Header</Text>
        </View>
    );
}
